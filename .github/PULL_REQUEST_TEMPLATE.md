## Why

<!-- The diff already says what. Say why: the reason is the part nobody can reconstruct
     in six months. A number beats an adjective. -->

## How it was verified

<!-- Not "the tests pass". What did you actually run, and what did it say?
     If you changed anything the bench covers, paste the gap. -->

- [ ] `npm test`
- [ ] `cd site && php artisan test`
- [ ] `cd site && vendor/bin/pint --test`
- [ ] Looked at it in a browser
- [ ] `npm run oracle` if the engine or the catalogue moved

## What this does not do

<!-- A limitation written here is a limitation. The same one discovered later is a bug.
     If a test cannot cover part of this, say so rather than letting the green tick
     imply otherwise. -->

## Files outside your own area

<!-- Several people work on this repository at once. If you touched a shared file
     (routes/web.php, forge.css, layout.blade.php, a migration on `schematics`),
     name it here. -->
