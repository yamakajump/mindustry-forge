{
  "name": "mindustry-forge",
  "private": true,
  "type": "module",
  "description": "Analyse a Mindustry schematic in the browser: what it makes, where it chokes, what it wastes",
  "scripts": {
    "test": "node --test \"tests/js/**/*.test.js\"",
    "oracle": "node tools/oracle.mjs",
    "oracle:measure": "node tools/oracle.mjs --measure"
  }
}
