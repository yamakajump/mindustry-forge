<?php

namespace App\Http\Controllers;

// Laravel's scaffolded base class, kept empty: no behaviour is shared across every
// controller in this app, so there is nothing to put here beyond the class every other
// controller extends.
abstract class Controller
{
    //
}
