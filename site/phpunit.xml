<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <!-- The work space size tests deliberately push payloads near Space::MAX_BOARD_BYTES
             (16 MiB since frames grew a board's realistic ceiling from 64x64 to 256x256);
             PHP's usual 128M CLI default is not enough headroom for that string plus the
             copies validation and json_encode make of it in the same process.

             It was 256M, and the suite outgrew it without anybody noticing, because CI's
             Linux PHP stays under that ceiling where a Windows build does not: `php artisan
             test` died on `SpaceController::assertBoardFits` while every pull request
             stayed green. Measured at 530 tests, 384M is enough and 256M is not; 512M is
             chosen so that the next few dozen tests do not silently reach it again. The
             suite runs in one process, so this grows with the suite. -->
        <ini name="memory_limit" value="512M"/>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="DB_URL" value=""/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
