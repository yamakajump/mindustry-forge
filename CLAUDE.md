# mindustry-forge

This repository's instructions for coding agents live in [`AGENTS.md`](AGENTS.md), the
vendor-neutral convention most agents read. Claude Code reads `CLAUDE.md` specifically, so
this file stays a pointer rather than a second copy that could drift from it.

@AGENTS.md
